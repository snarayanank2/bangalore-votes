---
/**
 * `/kn/account/submissions` — Kannada twin, see
 * src/pages/account/submissions.astro for the full explanation.
 */
import AccountSubmissions from '../../../features/pages/AccountSubmissions.astro';
import { loadSubmissions } from '../../../lib/account-flow';

const session = Astro.locals.session;
if (!session) {
  return Astro.redirect(`/login?next=${encodeURIComponent('/kn/account/submissions')}`);
}

const submissions = await loadSubmissions(session.userId);
Astro.response.headers.set('cache-control', 'no-store');
---

<AccountSubmissions lang="kn" submissions={submissions} />
